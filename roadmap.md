# Roadmap

- [x] Criar aba Funções (`FunctionFinder.tsx`) — cards das 4 funções + testador rápido + feedback
- [x] Criar aba FAST (`FastTool.tsx`) — 16 perguntas, pontuação automática, hipótese destacada
- [x] Registrar as duas abas no `Sidebar.tsx` (7 itens, nav mobile grid-cols-7)
- [x] Renderizar as abas no `dashboard.tsx`
- [ ] Verificação visual autenticada (pendente — login de teste recusado; build de produção passou)
